<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.elcatrin.customers">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" /> <!-- Permiso de internet -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" /> <!-- Permiso de ubicación -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.elcatrin.customers.activity.OrdersActivity"></activity>
        <activity
            android:name="com.elcatrin.customers.activity.BillingActivity"
            android:parentActivityName="com.elcatrin.customers.activity.methodPayment"
            android:screenOrientation="portrait"></activity>
        <activity
            android:name="com.elcatrin.customers.activity.methodPayment"
            android:parentActivityName="com.elcatrin.customers.activity.cashpaymentActivity"
            android:screenOrientation="portrait">
            android:parentActivityName=".activity.creditcardActivity"
        </activity>
        <activity android:name="com.elcatrin.customers.activity.creditcardActivity"
            android:screenOrientation="portrait"/>
        <activity
            android:name="com.elcatrin.customers.activity.cashpaymentActivity"
            android:parentActivityName="com.elcatrin.customers.activity.HomeActivity"
            android:screenOrientation="portrait"/>
        <!--
         The API key for Google Maps-based APIs is defined as a string resource.
         (See the file "res/values/google_maps_api.xml").
         Note that the API key is linked to the encryption key used to sign the APK.
         You need a different API key for each encryption key, including the release key that is used to
         sign the APK for publishing.
         You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name="com.elcatrin.customers.activity.CurrentLocationOnMap"
            android:label="@string/title_activity_current_location_on_map" />
        <activity android:name="com.elcatrin.customers.activity.CartActivity"
            android:screenOrientation="portrait"/>
        <activity
            android:name="com.elcatrin.customers.activity.StoresActivity"
            android:parentActivityName="com.elcatrin.customers.activity.ProductsActivity"
            android:screenOrientation="portrait"/>
        <activity
            android:name="com.elcatrin.customers.activity.ProductsActivity"
            android:parentActivityName="com.elcatrin.customers.activity.ProductDetailActivity"
            android:screenOrientation="portrait"/>
        <activity android:name="com.elcatrin.customers.activity.ProductDetailActivity"
            /> <!-- android:parentActivityName=".ui.CatalogoEmpresas" /> -->
        <activity
            android:name="com.elcatrin.customers.activity.HomeActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name="com.elcatrin.customers.activity.AuthActivity"
            android:parentActivityName="com.elcatrin.customers.activity.HomeActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>